# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

- for functions that has more than 2 parameters use a type and pass it in the function parameter
- use object destructuring rather than defining fields inside the function parameter
